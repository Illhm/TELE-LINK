# TELE-LINK

## Installation

Use standard npm install without extra CLI flags:

```bash
npm install
```

`npm install --A` forwards `-A` to lifecycle scripts and can fail in environments where `node` commands are shimmed (for example through Deno compatibility layers).

## Run

```bash
node server.js
```
